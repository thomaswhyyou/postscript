<div class="navbar navbar-default navbar-fixed-top">
  <div class="container">

    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to '<PostScript/>', root_path, class: 'navbar-brand' %>
    </div>

    <div class="navbar-collapse collapse">
      <% if user_signed_in? %>

        <ul class="nav navbar-nav navbar-right">
          <li>
            <a href='/' class='icon_menu'>Signed in::<%= current_user.name.upcase %></a>
          </li>

          <li>
            <a href='/' class='icon_menu'><icon class='icon-window'></icon></a>
          </li>

          <li>
            <%= link_to(edit_user_registration_path, class: 'icon_menu') do %>
              <icon class='icon-tools'></icon>
            <% end %>
          </li>

          <li>
            <%= link_to(destroy_user_session_path, class: 'icon_menu', method: 'delete') do %>
              <icon class='icon-logout'></icon>
            <% end %>
          </li>
        </ul>

      <% else %>

        <form class="navbar-form navbar-right">
          <%= link_to(user_omniauth_authorize_path(:github), class: 'btn btn-primary') do %>
            Sign in with<icon class='icon-github'></icon>
          <% end %>

        </form>
      <% end %>
    </div><!--/.nav-collapse -->
  </div>
</div>


